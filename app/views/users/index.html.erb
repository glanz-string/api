<%= @auth %>
<ul>
<% @users.each do |user| %>
<li><%= user[:name] %>, <%= user[:email] %>
  <%= link_to "delete", "/users/#{user[:id]}", method: :delete, data: { confirm: "You sure?" } %></li>
<% end %>
</ul>
